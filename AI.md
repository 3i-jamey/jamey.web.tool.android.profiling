# AI.md

이 저장소의 AI(너)는 프로젝트 루트 `.aimd/`를 공용 작업공간으로 쓴다.
**작업하며 생기는 계획·구조 문서·기록·결과는 전부 `.aimd/` 안에 남긴다**
**모든 대화·작업·산출물은 한글로 한다**(커밋 메시지만 영어).

## 1. 폴더 가이드

`.aimd/`는 십의 자리가 읽기 우선순위인 4계층이다. 아래 두 표가 전체 가이드다.

| 계층 | 읽기 | 수명·갱신 |
|---|---|---|
| **0x 기준** | 매 세션 전부 읽기 | 누적 |
| **1x 지식** | 필요할 때 열기 · `11-standard`는 해당 작업 시작 시 전부 | 누적 |
| **2x 작업** | 맡은 작업만 | 진행 중 · 소멸형 |
| **3x 결과** | 지목할 때만 · `31-adr`은 결정 전 훑기 | 불변 |

| 폴더 | 역할 | 주요 파일 | 읽기 |
|---|---|---|---|
| `01-instruction/` | 규칙·명령 | `01-coding.md` 코딩 가이드 · `02-reading.md` 리딩 가이드 | 시작 시 전부 |
| `02-reference/` | 필수 참조 | `01-dictionary.md` 공용 단어 규약 · `02-folder-map.md` repo 폴더 맵 | 시작 시 전부 |
| `11-standard/` | 상황별 규범(규약·양식·검증 기준) | `nn-{slug}.md` | 해당 작업 시작 시 전부 |
| `12-wiki/` | 살아있는 지식(서술·판정) | 주제 폴더 단위 `nn-{slug}/` | `INDEX.md`만 · 필요할 때 열기 |
| `21-plan/` | 계획·브리핑 | `yymmdd-nn-{slug}.md` | 작업 중 |
| `22-runtime/context/` | 진행 중 세션 맥락 | 세션당 디렉토리 · `01-overview.md` · `{nn}-{title}.md` | 작업 중 |
| `22-runtime/team/` | 엔진 실행 상태 | `{branch-key}/` · 소멸형 | 작업 중 |
| `31-adr/` | 닫힌 결정 | `yymmdd-nn-{slug}.md` · 불변 | 결정 전 제목 훑기 |
| `32-result/` | 닫힌 결과 | `yymmdd-nn-{slug}.md` · 불변 | 지목할 때만 |

## 2. 전체 규칙

- 저장소를 건드리기 전, 위 표의 `읽기` 열대로 읽는다 — `01-instruction/`은 번호 오름차순으로 빠짐없이 읽고 그 규칙·명령을 반드시 따른다.
- 0x(`01-instruction`+`02-reference`)는 합계 1,500줄 이내로 유지한다 — `01-dictionary.md`·`02-folder-map.md`는 합계에서 제외한다.

## 3. 쓰기 규칙

- 누적(0x·1x) 문서는 `nn-{slug}.md` 중요도 순(핵심이 `01`)으로 두고, 고쳐쓰기 우선으로 갱신한다 — 빌더·도큐멘터·피니셔만 역할 범위에서 직접 갱신한다.
- 기록(`21-plan`·`32-result`·`31-adr`)은 `yymmdd-nn-{slug}.md` — `nn`은 같은 날 순번, slug는 kebab-case.

## 4. 폴더별 규칙

- **`02-reference/`** — `01-dictionary.md`(100줄 이하)·`02-folder-map.md`(200줄 이하)는 무조건 두고, 폴더 변경은 같은 편집에서 맵을 갱신한다.
- **`11-standard/`** — 특정 작업 유형·도메인에만 적용되는 표준(규약·양식·검증 기준)을 둔다. 해당 작업을 시작하면 관련 문서를 전부 읽고 따르며, 모든 작업에 적용되면 `01-instruction` 승격, 따르지 않아도 되는 배경은 `12-wiki`.
- **`12-wiki/`** — **(!)** 문서는 전부 1단계 주제 폴더 안에 두고, 파일·폴더 변경 시 같은 편집에서 `INDEX.md`를 갱신한다 — 폴더·번호 규칙은 `INDEX.md` 머리말이 정한다. 판정형 문서(frontmatter `type: report`)는 주제당 1파일 고쳐쓰기·회차 원문은 `32-result`로 두고, `INDEX.md` 훅에는 판정값(점수·등급)을 두지 않는다.
- **`21-plan/`** — 파일 하나로 "무엇을 왜 어떻게"가 잡히게 쓴다.
- **`22-runtime/context/`** — 다음 세션이 이어받을 작업 맥락을 둔다. 디렉토리명은 세션 첫 사용자 프롬프트의 날짜와 제목으로 만든 `yymmdd-nn-{slug}`를 쓰며, `nn`은 같은 날 순번이고 `slug`는 첫 프롬프트 제목을 요약한 kebab-case다. 진입 파일 `01-overview.md`(목표·완료·미결·다음 단계·관련 파일)를 고쳐쓰기로 유지하고, 추가 문서는 `02-{title}.md`부터 번호 오름차순으로 두며 `title`은 자유로운 kebab-case를 쓴다. 작업이 닫히면 `32-result`에 정리한다.
- **`22-runtime/team/`** — `branch-key`는 브랜치명의 `/`를 `+`로 바꾼 경로용 값이다. 팀 실행 파일은 `AA-01-PLAN.md`처럼 대문자 plan id(`AA`~`ZZ` 순차) + 문서 타입이며, 세부는 팀 포맷이 정한다.
- **`31-adr/`** — 닫힌 결정만 불변 기록으로 쌓는다(본문: 결정·근거·기각 대안). 갱신할 수 있으면 `12-wiki`, 닫혀서 못 고치면 `31-adr`이며, 대체는 새 파일을 만들고 옛 파일엔 frontmatter `status: superseded`·`superseded_by`만 더한다.
- **`32-result/`** — 판단이 닫힌 사후 결과(분석·리뷰·조사·세션 기록)만 남긴다. 계속 유효한 결론은 같은 편집에서 증류한다 — 지식·판정은 `12-wiki`, 결정은 `31-adr`, 표준은 `11-standard`.
